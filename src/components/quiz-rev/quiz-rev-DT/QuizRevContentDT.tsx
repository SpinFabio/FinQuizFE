import React from 'react';

interface QuizRevContentDTProps {
  /* propName: propType */
}

const QuizRevContentDT: React.FC<QuizRevContentDTProps> = ({ /* props */ }) => {
  return (
    <div>
      /* content  Desktop  */
    </div>
  );
};

export default QuizRevContentDT;