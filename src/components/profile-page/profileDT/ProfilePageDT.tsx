import React from 'react';

interface ProfilePageDTProps {
  /* propName: propType */
}

const ProfilePageDT: React.FC<ProfilePageDTProps> = ({ /* props */ }) => {
  return (
    <div>
      /* profilo desktop non ancora implementato */
    </div>
  );
};

export default ProfilePageDT;