import React from 'react';

interface LoadingPageProps {
  /* propName: propType */
}

const LoadingPage: React.FC<LoadingPageProps> = ({ /* props */ }) => {
  return (
    <div className='bg-my-background w-full h-full'>

    </div>
  );
};

export default LoadingPage;