import React from 'react';

interface MacroRootDTProps {
  /* propName: propType */
}

const MacroRootDT: React.FC<MacroRootDTProps> = ({ /* props */ }) => {
  return (
    <div>
      /* Lista Desktop */
    </div>
  );
};

export default MacroRootDT;