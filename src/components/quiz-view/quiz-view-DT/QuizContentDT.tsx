import React from 'react';

interface QuizContentDTProps {
  /* propName: propType */
}

const QuizContentDT: React.FC<QuizContentDTProps> = ({ /* props */ }) => {
  return (
    <div>
      Content Desktop non ancora implementato  
    </div>
  );
};

export default QuizContentDT;