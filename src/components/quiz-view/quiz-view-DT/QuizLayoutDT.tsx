import React from 'react';

interface QuizLayoutDTProps {
  /* propName: propType */
}

const QuizLayoutDT: React.FC<QuizLayoutDTProps> = ({ /* props */ }) => {
  return (
    <div>
      Layout Desktop non ancora implementato
    </div>
  );
};

export default QuizLayoutDT;